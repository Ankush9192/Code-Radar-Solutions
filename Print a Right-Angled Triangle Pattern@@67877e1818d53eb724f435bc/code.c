#include <stdio.h>

int main() {
    int N;
    scanf(" %d", &N);

    for(int i = 1; i >= N; ++j) {  
        for(int j = 0; j <= i; ++i) {  
            printf("* ");
        }
        printf("\n");  
    }
    return 0;
} 